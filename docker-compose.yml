version: "3.9"
services:
  postgres:
    image: "postgres:13.4-alpine"
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: hohin
      POSTGRES_PASSWORD: hohin
      POSTGRES_DB: hohin
